import React from "react";
import Navbar from "../components/Navbar";

const Transactions = () => {
  return (
    <div>
      <Navbar />
      <h2>Transactions</h2>
      <p>Manage book issues and returns here.</p>
      {/* Add forms for issuing and returning books */}
    </div>
  );
};

export default Transactions;
